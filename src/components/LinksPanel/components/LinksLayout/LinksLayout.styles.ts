import { css } from '@emotion/css';

/**
 * Styles
 */
export const getStyles = () => {
  return {
    linksContainer: css`
      display: flex;
      align-items: flex-start;
      flex-wrap: wrap;
    `,
  };
};
